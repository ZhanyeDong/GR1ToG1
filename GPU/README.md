该文件夹是全部计算均使用GPU
使用build_sequence_bias_dataset获取高质量数据集，在train_files处修改输入数据集路径，在output修改输出数据集路径，在bias_abs设定随机范围，在target_samples设置目标数量
pinocchio_fk_ik_check是build_sequence_bias_dataset调用的用于清洗的函数，需要在example处更改urdf的文件位置
main_G1toGR1是将G1关节映射回GR1的纯由数据训练得到网络
